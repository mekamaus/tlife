#include "screen.h"
#include "vector.h"

void render_player(
    // screen
    Screen screen, const Dim2 *screen_size,
    // player
    const Pos2 *pos, const Pos2 *delta,
    // offset
    const Pos2 *offset);
