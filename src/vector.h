#pragma once

#include "scalar.h"

// Two-dimensional dimension vector (non-negative elements).
typedef struct Dim2 {
  Dim x;
  Dim y;
} Dim2;

// Two-dimensional position vector.
typedef struct Pos2 {
  Pos x;
  Pos y;
} Pos2;
